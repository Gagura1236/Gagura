# ============================================================
#  S&P 500 指數預測系統 — 套件需求清單（v1.1 更新）
#
#  ★ 安裝前請先確認系統依賴（macOS 使用者）：
#
#  【Step 0 — macOS 系統層級依賴（brew，非 pip，只需裝一次）】
#
#    Step 0a：安裝 Homebrew（若尚未安裝）
#      /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
#
#    Step 0b：安裝 OpenMP runtime（XGBoost 在 macOS 的必要系統依賴）
#      brew install libomp
#
#    完成後重新啟動終端機或 VSCode，再執行下方 pip install。
#
#  【Step 1 — 安裝 Python 套件】
#
#    終端機 / VSCode 終端機：
#      pip install -r requirements.txt
#
#    Jupyter Notebook / VSCode Notebook（在 Cell 1 執行）：
#      %pip install -r requirements.txt
#
#  注意：brew 安裝的是系統層級依賴，無法透過 pip 安裝，
#        且只需在每台電腦安裝一次。
# ============================================================

# ── 核心資料科學套件 ──────────────────────────────────────────
# 資料處理（DataFrame 操作、時間索引）
pandas>=2.0.0

# 數值計算（陣列運算、統計計算）
numpy>=1.24.0

# ── 金融資料 ─────────────────────────────────────────────────
# 從 Yahoo Finance 下載 S&P 500（^GSPC）與 VIX（^VIX）歷史資料
yfinance>=0.2.50

# ── 機器學習模型 ──────────────────────────────────────────────
# 提供 Random Forest、TimeSeriesSplit、MSE 等工具
scikit-learn>=1.3.0

# XGBoost 梯度提升樹模型
# ⚠️ macOS 需先執行：brew install libomp
xgboost>=2.0.0

# ── 視覺化：靜態 PNG 圖表 ─────────────────────────────────────
# 折線圖、長條圖、雙軸圖、散點圖、直方圖、CDF 等（版本 1.0 與 1.1 使用）
matplotlib>=3.7.0

# ── 統計工具 ─────────────────────────────────────────────────
# 版本 1.1 新增：計算誤差偏度（skew）、峰度（kurtosis）
scipy>=1.10.0

# ── Jupyter / VSCode Notebook 支援 ───────────────────────────
# 方法一（Jupyter Notebook）：啟動 jupyter notebook 指令需要
notebook>=7.0.0

# 方法四（VSCode Notebook）：VSCode Jupyter 擴充的 Python Kernel 後端
ipykernel>=6.0.0

# ── 互動式 HTML 報告 ──────────────────────────────────────────
# sp500_interactive_report.py 使用，輸出含懸停說明的 HTML 報告
# 圖表可縮放、可懸停查看精確數值，含術語詞彙表新視窗
plotly>=5.18.0
